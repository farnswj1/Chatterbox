import { FC } from 'react';

const Header: FC = () => (
  <header className="w-full p-4 flex justify-center items-center">
    <h1 className="text-4xl">Chatterbox</h1>
  </header>
);

export default Header;
